import styled from "styled-components";

export const HomeStyled = styled.div`
	margin: 0 auto;
	max-width: 100%;
	min-height: 100vh;
  background-color: var(--primary);
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  align-items: center;

  img {
    position: absolute;
    bottom: 0;
    left: 0;
  }
`;
